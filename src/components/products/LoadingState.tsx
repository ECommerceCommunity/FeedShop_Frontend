/**
 * LoadingState 컴포넌트
 *
 * 서버에서 상품 데이터를 가져오는 동안 표시되는 간단한 로딩 표시기 컴포넌트입니다.
 * 이 컴포넌트는 비동기 작업 중 사용자에게 시각적 피드백을 제공하여
 * 데이터 로딩 기간 동안의 혼란을 방지합니다.
 *
 * 주요 기능:
 * - 한국어 현지화가 적용된 중앙 정렬된 로딩 메시지
 * - 전체 디자인 시스템과 일관된 스타일링
 * - 가볍고 성능이 좋은 구현
 * - 대기 시간 동안 명확한 사용자 커뮤니케이션
 *
 * 사용 위치:
 * - 초기 로딩 중 상품 목록 페이지 (Lists.tsx)
 * - 데이터 가져오는 동안 상품 검색 결과
 * - 상품 관련 데이터 로딩 시나리오
 *
 * UX 고려사항:
 * - 시스템이 작동 중임을 즉시 피드백 제공
 * - 한국어 텍스트로 애플리케이션 언어와 일관성 유지
 * - 간단한 디자인으로 로딩 과정에서 주의 분산 방지
 * - 전환 중 레이아웃 안정성 유지를 위한 위치 설정
 */

import React from "react"; // 컴포넌트 생성을 위한 핵심 React 라이브러리
import { LoadingContainer } from "../../pages/products/Lists.styles"; // 로딩 상태 표시를 위한 스타일된 컨테이너

/**
 * LoadingState 함수형 컴포넌트
 *
 * 상품 데이터가 현재 가져오는 중임을 사용자에게 알리는
 * 중앙 정렬된 로딩 메시지를 렌더링합니다. 이 컴포넌트는
 * 좋은 사용자 경험을 유지하기 위해 비동기 작업 중
 * 플레이스홀더 역할을 합니다.
 *
 * @returns {JSX.Element} 한국어 로딩 메시지가 포함된 렌더링된 로딩 상태
 */
export const LoadingState: React.FC = () => {
  return (
    <LoadingContainer>
      {/* 로딩 메시지 - 상품이 로딩 중임을 알리는 한국어 텍스트 */}
      <div>상품을 불러오는 중...</div>
    </LoadingContainer>
  );
};
